import React from 'react';
import '../CSS/mainPage.css'; // Adjust the path based on your project structure
import Header from '../Components/Header';

function mainPage() {
    return (
        <div className="page">
            <Header/>
        </div>
    );
}

export default mainPage;